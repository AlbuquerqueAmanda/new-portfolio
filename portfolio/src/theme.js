import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: `'Roboto', sans-serif`, // ou qualquer outra que você queira
  },
});

export default theme;
